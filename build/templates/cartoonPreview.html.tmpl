<div class="row">
	<div class="twelve columns story">
		<div id="genre">
		{% for keyword in keywords %}
			<a href="#{{ keyword }}">{{ keyword }}</a>
		{% endfor %}
		</div>
		<div class="row">
			<div class="twelve columns toon">
				<a href="{{ path }}">
				{% if id < 10 %}
					<img id="cartoon" src="{{ src }}" alt="{{ caption }}" />
				{% else %}
					<a id="cartoon" class="inf_img" href="{{ src }}" rel="{{ caption }}"></a>
				{% endif %}
				</a>
				<p class="caption">{{ caption }}</p>
				<div id="meta" href="{{ path }}">
					<h3 class="author">by <span class="name">{{ author }}</span></h3>
					<h4 class="date">Published {{ publish_date }}</h4>
				</div>
			</div>
		</div>
	</div>
</div>